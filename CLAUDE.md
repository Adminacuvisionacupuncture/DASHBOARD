# EHI Dashboard

This folder is a git repo connected to the GitHub repo
`Adminacuvisionacupuncture/DASHBOARD` (remote `origin`, branch `main`), deployed via
Vercel at dashboard-one-gilt-54.vercel.app.

## Workflow policy

The user has asked that every edit made in this project be committed and pushed to
`origin/main` automatically — don't ask for confirmation before each push, just do it
as part of finishing the edit. Use short, descriptive commit messages.
